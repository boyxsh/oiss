<%= form_for(@company_introduction) do |f| %>
  <% if @company_introduction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company_introduction.errors.count, "error") %> prohibited this company_introduction from being saved:</h2>

      <ul>
      <% @company_introduction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :active %><br />
    <%= f.check_box :active %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
